# OpenAI Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-api-key-here

# Note: This .env file is loaded automatically by godotenv
# It's safe to commit .env.example but NEVER commit the actual .env file with real API keys
