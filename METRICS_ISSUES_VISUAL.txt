╔═════════════════════════════════════════════════════════════════════════════╗
║                 METRICS.GO - ISSUES VISUAL OVERVIEW                         ║
╚═════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🔴 CRITICAL: DUPLICATE COST TRACKING                                        │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────┐
    │  RecordLLMCall()         │
    │  metrics.go:244          │
    └──────────┬───────────────┘
               │
       ┌───────┴──────────────────────────┐
       │                                  │
   ┌───▼──────────────────────┐   ┌──────▼──────────────────┐
   │ SystemMetrics            │   │ UpdateCostMetrics()     │
   │ - TotalTokens            │   │ common/types.go:668     │
   │ - TotalCost              │   │                         │
   │ - SessionTokens    ◄─────┼──► - TotalTokens (DUPLICATE!)
   │ - SessionCost            │   │ - DailyCost             │
   │ - LLMCallCount           │   │ - CallCount             │
   └────────────────────────────   └─────────────────────────┘
                        ❌ TWO SOURCES OF TRUTH!

Impact:
  • Inconsistent token counts between system & agent level
  • Duplicate cost tracking logic
  • Difficult to maintain & debug

Fix: Consolidate into MetricsCollector


┌─────────────────────────────────────────────────────────────────────────────┐
│ 🔴 CRITICAL: DUPLICATE MEMORY TRACKING                                      │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────┐
    │  UpdateMemoryUsage()     │
    │  metrics.go:340          │
    └──────────┬───────────────┘
               │
       ┌───────┴──────────────────────────┐
       │                                  │
   ┌───▼──────────────────────┐   ┌──────▼──────────────────┐
   │ SystemMetrics            │   │ UpdateMemoryMetrics()   │
   │ - MemoryUsage (uint64)   │   │ common/types.go:690     │
   │ - MaxMemoryUsage  ◄──────┼──► - CurrentMemoryMB (int)  │
   │ - Average: ❌ NONE        │   │ - PeakMemoryMB   ◄──DUPLICATE!
   │ (bytes)                  │   │ - AverageMemoryMB ❌WRONG!
   └────────────────────────────   │ (MB)                    │
                                   └─────────────────────────┘
                        ❌ UNIT MISMATCH + LOGIC BUGS!

Issues:
  1. System tracks bytes, Agent tracks MB
  2. System has no average calculation
  3. Agent average calculation is WRONG (PeakMemoryMB * N / N = Peak!)
  4. Agent AverageCallDuration overwrites instead of averaging

Fix:
  1. Consolidate to metrics.go
  2. Fix average calculation formula


┌─────────────────────────────────────────────────────────────────────────────┐
│ 🔴 CRITICAL: DUPLICATE PERFORMANCE TRACKING                                 │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────┐
    │  RecordAgentExecution()      │
    │  metrics.go:181              │
    └──────────┬────────────────────┘
               │
       ┌───────┴──────────────────────────┐
       │                                  │
   ┌───▼──────────────────────┐   ┌──────▼──────────────────────┐
   │ metrics.AgentMetrics     │   │ UpdatePerformanceMetrics()  │
   │ - ExecutionCount ✓       │   │ common/types.go:725         │
   │ - SuccessCount ✓  ◄──────┼──► - SuccessfulCalls (DUP!)    │
   │ - ErrorCount ✓   ◄──────┼──► - FailedCalls (DUP!)        │
   │ - MinDuration ✓          │   │ - SuccessRate ✓            │
   │ - MaxDuration ✓          │   │ - ConsecutiveErrors ✓      │
   │ - AverageDuration ✓      │   │ - ErrorCountToday ✓        │
   │ - TimeoutCount ❌        │   │                            │
   └────────────────────────────   └────────────────────────────┘
                        ❌ DUPLICATE COUNTERS!

Duplication:
  ✓ = Tracked in both places
  ❌ = Missing in one place

Fix: Consolidate to MetricsCollector


┌─────────────────────────────────────────────────────────────────────────────┐
│ ❌ DEAD CODE: RecordToolExecution() - 70 Lines                              │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌───────────────────────────────────┐
    │  RecordToolExecution()            │
    │  metrics.go:109-178               │
    │                                   │
    │  Status: NEVER CALLED ❌          │
    │  Lines: 70 lines of dead code     │
    │  Dependencies: executionTracker,  │
    │               ExtendedExecutionMetrics
    └───────────────────────────────────┘
                      │
         ┌────────────┼────────────┐
         │            │            │
    ┌────▼─────┐  ┌───▼──────┐  ┌─▼──────────┐
    │execution  │  │Extended  │  │currentExec │
    │Tracker    │  │Execution │  │ution      │
    │(unused)   │  │Metrics   │  │(unused)   │
    │9 lines    │  │(unused)  │  │1 field    │
    │           │  │10 lines  │  │           │
    └─────────────  └──────────┘  └───────────┘

Action: DELETE ALL (90 lines of dead code)


┌─────────────────────────────────────────────────────────────────────────────┐
│ ⚠️  INEFFICIENT: Cache Hit Rate Calculation                                  │
└─────────────────────────────────────────────────────────────────────────────┘

    RecordCacheHit()
         │
    ┌────▼─────────────────────┐
    │  mc.CacheHits++          │
    │  updateCacheHitRate()    │ ◄─── Called EVERY TIME!
    └────┬─────────────────────┘      (millions of times possible)
         │
    RecordCacheMiss()
         │
    ┌────▼─────────────────────┐
    │  mc.CacheMisses++        │
    │  updateCacheHitRate()    │ ◄─── Called EVERY TIME!
    └──────────────────────────┘

Current:
  ├─ 1,000,000 cache hits
  ├─ 1,000,000 updateCacheHitRate() calls
  └─ 1,000,000 unnecessary calculations ❌

Fix: Calculate on-demand (lazy evaluation)
  ├─ 1,000,000 cache hits
  ├─ 0 updateCacheHitRate() calls during recording
  └─ 1 calculation when needed ✓


┌─────────────────────────────────────────────────────────────────────────────┐
│ 🐛 BUG: Wrong Memory Average Calculation                                     │
└─────────────────────────────────────────────────────────────────────────────┘

    UpdateMemoryMetrics called 5 times:

    Call 1: 100 MB  │ Total so far: 100  │ Average: 100
    Call 2: 150 MB  │ Total so far: 250  │ Average: 125
    Call 3: 120 MB  │ Total so far: 370  │ Average: 123
    Call 4: 200 MB  │ Total so far: 570  │ Average: 142
    Call 5: 80 MB   │ Total so far: 650  │ Average: 130 ✓

Current Formula (WRONG):
    AverageMemoryMB = (PeakMemoryMB * CallCount) / CallCount
                    = (200 * 5) / 5
                    = 200 (EQUALS PEAK!) ❌

Correct Formula:
    AverageMemoryMB = SUM(all memory) / CallCount
                    = 650 / 5
                    = 130 ✓


┌─────────────────────────────────────────────────────────────────────────────┐
│ 🐛 BUG: AverageCallDuration Only Stores LAST Duration                       │
└─────────────────────────────────────────────────────────────────────────────┘

    UpdateMemoryMetrics called 5 times:

    Call 1: 100ms duration │ AverageCallDuration = 100ms ✓
    Call 2: 200ms duration │ AverageCallDuration = 200ms (overwrites!)
    Call 3: 150ms duration │ AverageCallDuration = 150ms (overwrites!)
    Call 4: 120ms duration │ AverageCallDuration = 120ms (overwrites!)
    Call 5: 180ms duration │ AverageCallDuration = 180ms (FINAL VALUE)

    Result: 180ms (LAST value, not average!)
    Should be: (100+200+150+120+180) / 5 = 150ms ✓


┌─────────────────────────────────────────────────────────────────────────────┐
│ 📊 FUNCTION STATUS OVERVIEW                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

metrics.go Functions (21 total):

✅ OK (12):
   NewMetricsCollector         │ ResetSessionCost
   GetSessionCost              │ GetTotalCost
   LogCrewCostSummary          │ GetSystemMetrics
   ExportMetrics               │ exportJSON
   Reset                       │ Enable
   Disable                     │ IsEnabled

❌ DEAD (1):
   RecordToolExecution         (70 lines, not called)

⚠️  DUPLICATE (3):
   RecordAgentExecution        (duplicate with Agent methods)
   RecordLLMCall               (duplicate with UpdateCostMetrics)
   UpdateMemoryUsage           (duplicate with UpdateMemoryMetrics)

🔧 NEEDS FIX (3):
   RecordCacheHit              (inefficient)
   RecordCacheMiss             (inefficient)
   updateCacheHitRate          (inefficient)

🟡 COULD IMPROVE (2):
   exportPrometheus            (incomplete, inefficient)
   statusString                (simple, OK)


┌─────────────────────────────────────────────────────────────────────────────┐
│ 🎯 PRIORITY MATRIX                                                           │
└─────────────────────────────────────────────────────────────────────────────┘

            IMPACT
            HIGH    │         │
                    │ P0 ▲    │ P1 ▲
                    │(3)│(4)  │(6)
                    │   │ ▲   │
            MEDIUM  │         │
                    │    P2 ▲ │
                    │    (2)  │
                    │         │ P3
            LOW     │         │ (1)
                    └─────────┴──────────
                    LOW      MEDIUM   HIGH
                         EFFORT

P0 CRITICAL (Delete + Fix Critical Bugs):
   (1) Delete RecordToolExecution
   (2) Fix memory average calculation
   (3) Fix AverageCallDuration logic
   Time: ~3 hours

P1 HIGH (Consolidate Duplicates):
   (4) Consolidate cost tracking
   (5) Consolidate performance tracking
   (6) Consolidate memory tracking
   Time: ~6 hours

P2 MEDIUM (Optimize):
   (7) Fix cache hit rate efficiency
   Time: ~1 hour

P3 LOW (Nice-to-Have):
   (8) Improve Prometheus export
   Time: ~2 hours

═══════════════════════════════════════════════════════════════════════════════
